## Tasks
- [x] 1. Add `load(tenant_id)` classmethod to `XhsConfig` in `config.py`
- [x] 2. Remove unused `text` import from `xhs_tasks.py`
- [x] 3. Verify frontend build still passes (no frontend changes — N/A)
