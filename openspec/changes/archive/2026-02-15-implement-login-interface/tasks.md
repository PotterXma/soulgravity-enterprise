## 1. Global Styles

- [x] 1.1 Add `.glass-panel` and autofill override styles to `src/index.css` to enable the "Ethereal Nebula" aesthetic.

## 2. Feature Components

- [x] 2.1 Create `src/features/auth/` directory.
- [x] 2.2 Implement `src/features/auth/LoginPage.tsx` with Glassmorphism UI, `framer-motion` animations, and integration with `useAuthStore` and `BackgroundBlobs`.

## 3. Integration & Routing

- [x] 3.1 Update `src/main.tsx` or `src/App.tsx` to route `/login` to `LoginPage` (if routing exists) or replace the mock landing page. *Note: For this iteration, we will likely replace the current scaffold content.*
- [x] Verify Login Flow <!-- id: 4 -->
